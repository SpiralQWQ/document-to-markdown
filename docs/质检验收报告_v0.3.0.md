# 验收报告 v0.3.0 — qa_runner 转写质检系统

> 日期：2026-08-12
> 项目：M.AIStudy 文档转写工具（_mineru_tools）
> 范围：新增 `qa/` 质检包（L1 自动 / L2 表格复核 / L3 视觉+文本复审 / 返工闭环 / 三色报告）
> 前置：全量重转工程完成（275 唯一块，复杂186⊂普通275，云端+OCR，0 失败）
> 流程：S1 穷举闭包任务清单 → S2 防呆全路径 → S3 逐 Task 4 轮审核 → S4 全量回归

## 一、交付物

新增 `_mineru_tools/qa/` 质检包（10 文件，全部语法通过）：

| 文件 | 职责 |
|---|---|
| `qa_runner.py` | 根入口 shim |
| `qa/runner.py` | CLI 主控（list/l1/l2/l3/rework/report 6 命令） |
| `qa/blocks.py` | 块清单读取（去重）+ out_dir 计算 + 状态扫描 |
| `qa/l1.py` | L1：完整性/双层页数/md_lint(高/低优先级)/图片引用 |
| `qa/l2.py` | L2：camelot 表格复核（表格密集块） |
| `qa/l3.py` | L3：公式密集标记/文本层/判定规则 |
| `qa/vision.py` | fitz 渲染 + vision_analyzer 双视觉集成 |
| `qa/sample.py` | L3 抽样（高优先全查+公式代表）+ 成本预估 |
| `qa/rework.py` | 返工清单 + 重转命令生成 |
| `qa/report.py` | 三色报告（JSON+MD） |

## 二、Task 完成情况（18/18）

| 阶段 | Task | 完成 | 审核轮次 | 抓到的真实缺陷 |
|---|---|---|---|---|
| 骨架 | 01 骨架+清单 | ✅ | 4轮 | argparse 子解析器覆盖 bug |
| | 02 块状态 | ✅ | 4轮 | 缺字段整批崩溃×2 |
| L1 | 03 完整性+页数 | ✅ | 4轮 | origin.pdf UUID 命名、空白页误报、slice 缺失漏报 |
| | 04 md_lint 集成 | ✅ | 4轮 | 噪声标记未分级 |
| | 05 图片引用 | ✅ | 4轮 | — |
| | 06 待复审队列 | ✅ | 4轮 | — |
| L2 | 07 camelot 表格 | ✅ | 4轮 | — |
| | 08 TEDS | ✅(否决) | — | 无真值/与L3重复/依赖缺失，已评估否决 |
| | 09 公式密集标记 | ✅ | 4轮 | latex_markers 重复计数 |
| L3 | 10 文本层 | ✅ | 4轮 | — |
| | 11 fitz 渲染 | ✅ | 4轮 | doc.close() 后访问 f-string bug |
| | 12 视觉集成 | ✅ | 4轮 | — |
| | 13 A+B 组合审 | ✅ | 4轮 | — |
| | 14 判定规则 | ✅ | 4轮 | — |
| | 15 抽样+成本 | ✅ | 4轮 | — |
| 闭环 | 16 返工闭环 | ✅ | 4轮 | 重转命令漏 --complex |
| | 17 报告生成 | ✅ | 4轮 | — |
| 防呆 | 18 防呆总验 | ✅ | 4轮 | report 缺队列出假报告(严重) |

**4 轮审核共抓出 12 个真实缺陷并修复**（每轮审核后重验才进下个 Task）。

## 三、数据真相与验证结果

- **块数真相**：plan.json 的 `pending_complex`(186) 是 `pending_normal`(275) 的子集 → 唯一块 **275**（此前误以为 461）。`load_blocks(all)` 已去重。
- **L1 全量**：275 块完成，**高优先真问题 5**（2 乱码 + 3 代码围栏奇数），低优先候选 190，缺输出 0。
- **L2**：73 表格密集块 → **13 表格标红**（camelot 低置信/漏检）。
- **L3 实测**：A+B 双视觉真实跑通，抓到 MinerU content_list 类型误判（TOC 条目标成 table）。
- **转写**：275 块云端+OCR 全转完，0 失败。

## 四、S4 全量回归（全部通过）

`list`(275) → `l1`(5高/190低) → `l2`(73→13标红) → `l3`(15目标/~315调用) → `rework`(5块+4命令) → `report`(80放心/5返工/190候选)。全部模块 `py_compile` 通过。

## 五、已知局限

1. md_lint 表格启发式（`|` 判断）噪声大 → 已降为「低优先候选」，需 L2/L3 复审
2. camelot 只查 PDF 第 1 页（抽样，非全量）
3. `$` 奇数对代码书误报（正则/Shell `$`）→ 标记待复审不判错
4. L3 视觉关键词判定为启发式（偏保守，suspicious 进人工）
5. quality_spec 问题归类为粗略映射（代码围栏→标题层级）

## 六、产物位置

- 待复审队列：`_data/qa/l1_queue.json`
- L2 结果：`_data/qa/l2_results.json`
- 三色报告：`_data/qa/qa_report.json` + `qa_report.md`
- 返工清单：`_data/qa/rework.json`

## 七、结论

**验收通过**：qa_runner v0.3.0 完成 18 个最小单元、12 个缺陷修复、防呆全路径验证、S4 回归全过。可进入正式使用（`python qa_runner.py l1` → `l2` → `l3` → `rework` → `report`）。

## 八、执行结果补充（2026-08-12 全量 L3 + 返工）

**L3 全量视觉复审**（15 目标 / 45 页 / 双视觉）：
- verdict 分布：**pass 1 / suspicious 14**（`$` 误报纠正后无 fail）
- ✅ Python灰帽子(p1-157)：视觉确认**通过**，澄清「代码围栏 379 奇数」为 md_lint 误报
- 📌 白话深度学习与TensorFlow、编写高质量代码：`$` 奇数由 fail 纠正为 **suspicious**（代码+公式混合书启发式局限，需人工最终确认）

**返工闭环**：
- Python学习手册 7 块：重转后**乱码全消除** ✅
- 编写高质量代码 p1-190：21 乱码为 MinerU OCR 固有(漏字 `__getattribute__` 的 'g')，源 PDF 干净，重转复现 → 0.009% 可忽略，可选文本修复

**最终三色报告**：放心 81 / 返工 0 / 候选 194（275 块）
